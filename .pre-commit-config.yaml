---
# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
  ##############################################################################
  ################################ General Checks ##############################
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        exclude: .gitignore
      - id: end-of-file-fixer
      - id: check-yaml
        exclude: mkdocs.yml
      - id: check-added-large-files
        args: [--maxkb=1500]
      - id: check-case-conflict
      - id: check-json
      - id: check-merge-conflict
      - id: debug-statements
      - id: detect-private-key
  ##############################################################################
  ################################ Security Checks #############################
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        name: Detect secrets
        args:
          - --disable-plugin
          - KeywordDetector
          - --disable-plugin
          - Base64HighEntropyString
          - --exclude-secrets
          - "[0-9a-f]{40}" # commit shas
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.0
    hooks:
      - id: gitleaks
  ##############################################################################
  ################################ YAML Checks #################################
  - repo: https://github.com/google/yamlfmt
    rev: v0.21.0
    hooks:
      - id: yamlfmt
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.38.0
    hooks:
      - id: yamllint
  ##############################################################################
  ################################ Lua Checks ##################################
  # Vendored `Lazyvim/` reference tree is excluded via the files filter.
  - repo: local
    hooks:
      - id: stylua
        name: stylua
        entry: stylua --check
        language: system
        files: ^(lua|after|ftdetect)/.*\.lua$
      - id: selene
        name: selene
        entry: selene
        language: system
        files: ^(lua|after|ftdetect)/.*\.lua$
##############################################################################
################################ Linting Checks #############################
# - repo: local
#   hooks:
#     - id: prettier
#       name: prettier-local
#       entry: bunx prettier --config .prettierrc.json --check .
#       language: system
#       types: [file]
#       pass_filenames: true
##############################################################################
################################ Shell Checks ################################
# - repo: https://github.com/gruntwork-io/pre-commit
#   rev: v0.1.24
#   hooks:
#     - id: shellcheck
